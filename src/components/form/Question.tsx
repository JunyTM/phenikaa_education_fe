import React from "react";

type Props = {
    // id: number;
    // title: string;
    // isPublic: boolean;
};

const Question = (props: Props) => {
  return <div>Question</div>;
};

export default Question;
